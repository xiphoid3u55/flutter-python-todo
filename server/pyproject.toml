[tool.poetry]
name = "server"
version = "0.1.0"
description = "server for todo list"
authors = ["xiphoid3u55 <ugly.kid.joe@live.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
